# Core dependencies for Cross-Cultural Mel-Scale Audio Frontend Bias Research
# ICASSP 2026 Paper Implementation

# Deep learning framework
torch>=1.12.0            # PyTorch for model implementation
torchaudio>=0.12.0       # PyTorch audio processing

# Audio processing and analysis
soundfile>=0.12.1        # Audio I/O (required for CommonVoice, GTZAN)
librosa>=0.10.0          # Audio processing utilities and transforms
scipy>=1.8.0             # Scientific computing (for ERB, Bark scales)

# Dataset access and management
datasets>=2.12.0         # Hugging Face datasets (CommonVoice, GTZAN)
huggingface-hub>=0.15.0  # HF authentication and access
requests>=2.28.0         # Direct downloads (Zenodo, FMA)
kaggle>=1.5.0            # Kaggle datasets (Carnatic, Hindustani)

# Data handling and preprocessing
pandas>=1.4.0            # Metadata management and CSV processing
numpy>=1.21.0            # Numerical operations and array handling
scikit-learn>=1.1.0      # ML utilities, train/test splits, metrics

# Progress tracking and visualization
tqdm>=4.64.0             # Progress bars for training and data loading
matplotlib>=3.5.0        # Plotting and visualization
seaborn>=0.11.0          # Statistical data visualization

# Experiment tracking (optional but recommended)
tensorboard>=2.9.0       # TensorBoard logging
# wandb>=0.12.0          # Weights & Biases (uncomment if using)

# System utilities (built-in with Python 3.4+)
# pathlib                # Path handling
# zipfile                # Archive extraction  
# tarfile                # Archive extraction
# json                   # JSON file handling
# logging                # Logging utilities

# Optional: Advanced audio processing (for future full implementation)
# torch>=1.12.0          # PyTorch (when models are released)
# scipy>=1.7.0           # Scientific computing