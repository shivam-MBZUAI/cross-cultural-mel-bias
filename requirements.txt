# Core dependencies for dataset downloading
datasets>=2.12.0         # Hugging Face datasets (CommonVoice, GTZAN)
huggingface-hub>=0.15.0  # HF authentication and access
requests>=2.28.0         # Direct downloads (Zenodo, FMA)
kaggle>=1.5.0            # Kaggle datasets (Carnatic, Hindustani)

# Audio processing
soundfile>=0.12.1        # Audio I/O (required for CommonVoice, GTZAN)
librosa>=0.10.0          # Audio processing utilities
torchaudio>=0.12.0       # PyTorch audio (dataset loading)

# Data handling
pandas>=1.4.0            # Metadata management
numpy>=1.21.0            # Numerical operations

# System utilities
pathlib                  # Path handling (built-in Python 3.4+)
zipfile                  # Archive extraction (built-in)
tarfile                  # Archive extraction (built-in)

# Optional: Advanced audio processing (for future full implementation)
# torch>=1.12.0          # PyTorch (when models are released)
# scipy>=1.7.0           # Scientific computing
# scikit-learn>=1.0.0    # Machine learning utilities